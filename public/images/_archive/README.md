# Archived reference images

Photos kept for design reference or future chapters. **Not referenced by app code** — safe to browse but do not wire into `lib/sections/` or components without moving back to an active folder.

Promote an asset: move it to the appropriate `public/images/<topic>/` path, add `imageAlt` + code reference, run `npm run audit:assets`.
